spring.cloud.stream:
  function:
    definition: itemProcessor
  # Processors
  bindings:
    # green
    itemProcessor-in-0:
      destination: demo-cqrs-order-validated
    itemProcessor-out-0:
      destination: demo-cqrs-cheap-item
    itemProcessor-out-1:
      destination: demo-cqrs-affordable-item
    itemProcessor-out-2:
      destination: demo-cqrs-expensive-item
  kafka:
    streams:
      binder:
        applicationId: kafka-cqrs-query-processor
        configuration:
          schema.registry.url: http://localhost:8081
          commit.interval.ms: 100
          default:
            key.serde: org.apache.kafka.common.serialization.Serdes$StringSerde
            value.serde: io.confluent.kafka.streams.serdes.avro.SpecificAvroSerde

server.port: 9002